---
apiVersion: v2
name: postgres
description: >-
  OwnSuite PostgreSQL via CloudNativePG (ADR-004): one single-instance Cluster
  plus one managed role + Database per app (1 db/app). Owner passwords come from
  Secrets generated by platform-configuration. No backups yet — PITR to S3 lands
  in Phase 3 (ADR-006).
type: application
version: 0.1.0
appVersion: "1"

apiVersion: v1
kind: Service
metadata:
  name: grist
  labels:
    app.kubernetes.io/name: grist
spec:
  selector:
    app.kubernetes.io/name: grist
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http

# Dev/CI toolchain for the Ansible bootstrap and its tests.
# Pinned (AGENTS.md: never `latest`). Kept separate from requirements-docs.txt.
ansible-core==2.21.1
ansible-lint==26.4.0
yamllint==1.38.0
molecule==26.4.0
molecule-plugins[docker]==25.8.12
pytest==9.1.1
pytest-testinfra==10.2.2
# Linter/formatter for the `suite` installer package + its tests (Phase 4).
ruff==0.15.19
